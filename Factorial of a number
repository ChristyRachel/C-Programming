#include<stdio.h>
int main()
{
  int limit, i, factorial = 1;
  
  printf("Enter the limit : ");
  scanf("%d", &limit);
  
  for(i = 1; i <=limit; i++)
  {
     factorial = factorial * i;
  }
  
  printf("factorial of %d is %d", limit,factorial);
  return 0;
}
