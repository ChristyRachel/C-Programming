#include <stdio.h>

int main()
{
    int count,number;
    printf("Enter the integer : ");
    scanf("%d",&number);
    for(count=1;count<=100;count++)
    {
        printf("%d * %d = %d\n",number,count,number*count);
    }

    return 0;
}
